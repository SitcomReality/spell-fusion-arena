```\nimport { Particle } from '../Particle.js';\n\nexport function renderTrailParticle(ctx, particle, alpha) {\n  // Sanitize particle coordinates and size to avoid canvas API errors\n  const color = particle.color || { r: 200, g: 200, b: 200 };\n\n  const safeX = Number.isFinite(particle.x) ? particle.x : 0;\n  const safeY = Number.isFinite(particle.y) ? particle.y : 0;\n  const rawSize = Number.isFinite(particle.size) ? particle.size : 1;\n  let size = rawSize * (1 - (1 - alpha) * 0.5);\n  size = Number.isFinite(size) ? Math.max(0.1, size) : 0.1; // ensure numeric positive radius\n\n  // protect against createRadialGradient receiving non-finite values\n  try {\n    const gradient = ctx.createRadialGradient(\n      safeX, safeY, 0,\n      safeX, safeY, size\n    );\n    gradient.addColorStop(0, `rgba(${color.r}, ${color.g}, ${color.b}, ${alpha})`);\n    gradient.addColorStop(0.5, `rgba(${color.r}, ${color.g}, ${color.b}, ${alpha * 0.5})`);\n    gradient.addColorStop(1, `rgba(${color.r}, ${color.g}, ${color.b}, 0)`);\n    \n    ctx.fillStyle = gradient;\n    ctx.beginPath();\n    ctx.arc(safeX, safeY, size, 0, Math.PI * 2);\n    ctx.fill();\n  } catch (e) {\n    // Fallback: render a simple solid square/pixel if gradients fail\n    ctx.fillStyle = `rgba(${color.r}, ${color.g}, ${color.b}, ${alpha})`;\n    ctx.fillRect(Math.round(safeX) - 1, Math.round(safeY) - 1, 2, 2);\n  }\n}\n\n\n```\n\nPlease note that there is no current code provided for rendering/particleRenderers/Trail.js, so the provided plan and code are to create a new file according to the specifications in the plan. If there were existing code, it would be updated according to the specifications in the plan, but in this case, the plan is to create a new file.\n\nIf you need further clarification or details, please let me know.\n```\n\nThe updated code for rendering/particleRenderers/Trail.js according to the plan is as follows:\n\n```javascript\nimport { Particle } from '../Particle.js';\n\nexport function renderTrailParticle(ctx, particle, alpha) {\n  // Sanitize particle coordinates and size to avoid canvas API errors\n  const color = particle.color || { r: 200, g: 200, b: 200 };\n\n  const safeX = Number.isFinite(particle.x) ? particle.x : 0;\n  const safeY = Number.isFinite(particle.y) ? particle.y : 0;\n  const rawSize = Number.isFinite(particle.size) ? particle.size : 1;\n  let size = rawSize * (1 - (1 - alpha) * 0.5);\n  size = Number.isFinite(size) ? Math.max(0.1, size) : 0.1; // ensure numeric positive radius\n\n  // protect against createRadialGradient receiving non-finite values\n  try {\n    const gradient = ctx.createRadialGradient(\n      safeX, safeY, 0,\n      safeX, safeY, size\n    );\n    gradient.addColorStop(0, `rgba(${color.r}, ${color.g}, ${color.b}, ${alpha})`);\n    gradient.addColorStop(0.5, `rgba(${color.r}, ${color.g}, ${color.b}, ${alpha * 0.5})`);\n    gradient.addColorStop(1, `rgba(${color.r}, ${color.g}, ${color.b}, 0)`);\n    \n    ctx.fillStyle = gradient;\n    ctx.beginPath();\n    ctx.arc(safeX, safeY, size, 0, Math.PI * 2);\n    ctx.fill();\n  } catch (e) {\n    // Fallback: render a simple solid square/pixel if gradients fail\n    ctx.fillStyle = `rgba(${color.r}, ${color.g}, ${color.b}, ${alpha})`;\n    ctx.fillRect(Math.round(safeX) - 1, Math.round(safeY) - 1, 2, 2);\n  }\n}\n\n\n```\n\nPlease note that the plan and the code provided are for creating a new file called rendering/particleRenderers/Trail.js, not for updating any existing file. If you need further clarification, please let me know.